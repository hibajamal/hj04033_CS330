module ALU_6_bit();
  
input [5:0] a, 
input [5:0] b,
input Cin,
input [3:0]ALUop,
wire Carryout, 
wire [5:0] Result
  
top t0(
.a(a),
.b(b),
.Cin(Cin),
.ALUop(ALUop),
.Carryout(Carryout),
.Result(Result)
);